# 小汪的日常

一个本地运行的个人计划网站，包含每日待办、长期规划、成功日记、心得和数据看板。数据使用 SQLite 保存在本机。

## 启动

macOS 双击 `启动小汪的日常.command`，或在项目目录运行：

```bash
python3 -B server.py
```

然后访问 [http://127.0.0.1:8765](http://127.0.0.1:8765)。

## 数据

个人数据保存在 `data/xiaowang.sqlite3`。`data` 目录已被 Git 忽略，不会提交到远程仓库。
